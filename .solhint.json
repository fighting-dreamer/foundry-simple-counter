{
  "extends": "solhint:recommended",
  "rules": {
    "compiler-version": ["error", "^0.8.26"],
    "func-visibility": ["error", { "ignoreConstructors": true }],
    "comment_01": "// Disable formatting rules that conflict with Prettier",
    "indent": "off",
    "max-line-length": "off",
    "quotes": "off"
  }
}
